# 合同起草模块 — 完整实施方案

> **适用版本**：CoPaw 源码版（`copaw >= 0.0.4b2`，agentscope 环境）  
> **目标**：在一个全新的 CoPaw 源码环境中，从零复现合同起草功能。  
> **预计耗时**：30 分钟

---

## 目录

1. [架构总览](#1-架构总览)
2. [文件清单](#2-文件清单)
3. [第一步：修改 CoPaw 框架源码](#3-第一步修改-copaw-框架源码)
4. [第二步：创建 shared 公共模块](#4-第二步创建-shared-公共模块)
5. [第三步：创建 Skills](#5-第三步创建-skills)
6. [第四步：创建 Python Tools](#6-第四步创建-python-tools)
7. [第五步：注册 Tools 到框架](#7-第五步注册-tools-到框架)
8. [第六步：创建 Mock 模板 API 服务](#8-第六步创建-mock-模板-api-服务)
9. [第七步：同步 Skills 并启动服务](#9-第七步同步-skills-并启动服务)
10. [验证测试](#10-验证测试)
11. [生产环境替换说明](#11-生产环境替换说明)

---

## 1. 架构总览

```
用户对话（"帮我起草一份采购合同"）
        │
        ▼
┌─────────────────────────────────────────────────┐
│  contract_draft  主编排 Skill（SKILL.md）        │  ← LLM 驱动流程编排
│  1. 提取关键词 → 调用模板匹配脚本               │
│  2. 展示模板列表 → 等待用户确认                 │
│  3. 调用参数提取 Skill → 展示表单               │
│  4. 调用起草 LLM Skill → 生成 Markdown 合同     │
│  5. 调用 markdown_to_word 工具 → 生成 .docx     │
│  6. 调用 save_contract_file 工具 → 保存文件     │
└─────────────────────────────────────────────────┘
        │                          │
        ▼                          ▼
┌──────────────────┐     ┌───────────────────────┐
│ 子 Scripts       │     │  Python Tools          │
│ template_match   │     │  markdown_to_word      │
│ .py              │     │  save_contract_file    │
│  ├─ 调用 Mock API│     └───────────────────────┘
│  ├─ push() 推送  │
│  └─ save_log()   │
└──────────────────┘
        │
        ▼
┌─────────────────────────┐
│  shared 公共模块         │
│  push.py → HTTP POST    │  →  CoPaw /api/console/internal-push
│  db.py   → SQLite       │  →  ~/.copaw/agent_logs.db
└─────────────────────────┘
```

**关键设计原则**：
- Skill 脚本是**独立子进程**，无法直接访问 CoPaw 内存，通过 HTTP 回环推送消息
- `subscribe_id = session_id + "_" + user_id`，前端按此 ID 轮询消息
- 所有子智能体执行日志写入 SQLite，字段：`run_id / exec_id / agent_id / input_content / output_content / runtime`

---

## 2. 文件清单

需要创建/修改以下文件（均在 CoPaw 源码根目录 `src/copaw/` 下）：

```
src/copaw/
├── app/
│   ├── _app.py                          ← 修改：挂载 /files/contracts 静态路由
│   └── routers/
│       └── console.py                   ← 修改：新增 /internal-push 端点
├── agents/
│   ├── tools/
│   │   ├── __init__.py                  ← 修改：注册新 tools
│   │   ├── markdown_to_word.py          ← 新建：Markdown → Word 工具
│   │   └── save_contract_file.py        ← 新建：Word 文件存储工具
│   ├── react_agent.py                   ← 修改：导入并注册新 tools
│   └── skills/
│       ├── shared/                      ← 新建目录
│       │   ├── __init__.py
│       │   ├── push.py                  ← 新建：WebSocket 推送工具
│       │   ├── db.py                    ← 新建：日志持久化工具
│       │   └── mock_template_api.py     ← 新建：Mock API 服务
│       ├── contract_draft/
│       │   └── SKILL.md                 ← 新建：主编排 skill
│       ├── contract_template_match/
│       │   ├── SKILL.md                 ← 新建
│       │   └── scripts/
│       │       └── template_match.py    ← 新建
│       ├── contract_template_params/
│       │   └── SKILL.md                 ← 新建
│       └── contract_draft_llm/
│           └── SKILL.md                 ← 新建
scripts/
└── start_contract_services.sh           ← 新建：一键启动脚本
```

---

## 3. 第一步：修改 CoPaw 框架源码

### 3.1 修改 `src/copaw/app/routers/console.py`

在文件末尾**追加**以下代码（保留原有 `get_push_messages` 路由不变）：

```python
# ── 在文件末尾追加 ──────────────────────────────────────────────────────────

class InternalPushRequest(BaseModel):
    session_id: str
    user_id: str
    text: str
    msg_type: str = "progress"
    subscribe_id: str | None = None


@router.post("/internal-push")
async def internal_push(req: InternalPushRequest):
    """Internal endpoint for skill scripts (subprocesses) to push real-time messages.

    Skills scripts call this via HTTP loopback so their progress is visible
    in the chat frontend. subscribe_id = session_id + "_" + user_id.
    """
    from ..console_push_store import append

    label = {"progress": "⏳", "result": "✅", "error": "❌", "chain": "🔗"}.get(
        req.msg_type, "📌"
    )
    text = req.text if req.text.startswith(tuple("⏳✅❌🔗📌🤖📋📄🔍")) else f"{label} {req.text}"
    await append(req.session_id, text)
    return {"ok": True}
```

同时在文件**顶部** `from fastapi import APIRouter, Query` 后面补充：

```python
from pydantic import BaseModel
```

### 3.2 修改 `src/copaw/app/_app.py`

找到以下代码块：

```python
app.include_router(api_router, prefix="/api")
app.include_router(
    agent_app.router,
    prefix="/api/agent",
    tags=["agent"],
)

# Mount console: root static files (logo.png etc.) then assets, then SPA
```

在两个 `include_router` 之后、注释 `# Mount console` 之前，插入：

```python
# Mount contract files directory for frontend access (Word preview/download)
_contracts_dir = Path(os.environ.get(
    "CONTRACT_STORAGE_DIR",
    str(Path.home() / ".copaw" / "contracts"),
))
_contracts_dir.mkdir(parents=True, exist_ok=True)
app.mount(
    "/files/contracts",
    StaticFiles(directory=str(_contracts_dir)),
    name="contracts",
)
```

> **说明**：`Path` 和 `StaticFiles` 在该文件中已有导入，无需额外添加。

---

## 4. 第二步：创建 shared 公共模块

### 4.1 创建目录

```bash
mkdir -p src/copaw/agents/skills/shared
```

### 4.2 创建 `src/copaw/agents/skills/shared/__init__.py`

```python
# -*- coding: utf-8 -*-
```

### 4.3 创建 `src/copaw/agents/skills/shared/push.py`

```python
# -*- coding: utf-8 -*-
"""WebSocket push utility for skill scripts.

Scripts (subprocesses) import this and call push() to send real-time
progress messages to the frontend via the CoPaw internal HTTP loopback.

subscribe_id = session_id + "_" + user_id  (frontend subscribes by this key)
"""
import os
import requests

COPAW_API = os.environ.get("COPAW_API_BASE", "http://127.0.0.1:8088")


def push(session_id: str, user_id: str, text: str, msg_type: str = "progress") -> None:
    """Push a real-time message to the frontend.

    Args:
        session_id:  Current conversation session ID (passed via sys.argv[1])
        user_id:     Logged-in user ID (passed via sys.argv[2])
        text:        Message text (Markdown supported)
        msg_type:    progress | result | error | chain
    """
    subscribe_id = f"{session_id}_{user_id}"
    try:
        requests.post(
            f"{COPAW_API}/console/internal-push",
            json={
                "subscribe_id": subscribe_id,
                "session_id": session_id,
                "user_id": user_id,
                "text": text,
                "msg_type": msg_type,
            },
            timeout=3,
        )
    except Exception:
        pass  # push failure must not break the main script
```

### 4.4 创建 `src/copaw/agents/skills/shared/db.py`

```python
# -*- coding: utf-8 -*-
"""Agent log persistence utility for skill scripts.

Writes agent execution records to SQLite (dev) or PostgreSQL (prod).
Scripts import this and call save_agent_log() to persist input/output/runtime.

Fields map to spec:  runId → run_id,  execId → exec_id,  agentID → agent_id,
                     inputContent → input_content,  outputContent → output_content,
                     runtime → runtime (ms)
"""
import json
import os
import sqlite3
from pathlib import Path

DB_PATH = os.environ.get(
    "AGENT_LOG_DB",
    str(Path.home() / ".copaw" / "agent_logs.db"),
)


def _get_conn() -> sqlite3.Connection:
    conn = sqlite3.connect(DB_PATH)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS agent_logs (
            id             INTEGER PRIMARY KEY AUTOINCREMENT,
            run_id         TEXT NOT NULL,
            exec_id        TEXT NOT NULL,
            agent_id       TEXT NOT NULL,
            input_content  TEXT,
            output_content TEXT,
            runtime        INTEGER,
            error_msg      TEXT,
            extra          TEXT,
            created_at     TEXT DEFAULT (datetime('now','localtime'))
        )
    """)
    conn.execute("CREATE INDEX IF NOT EXISTS idx_exec_id ON agent_logs(exec_id)")
    conn.execute("CREATE INDEX IF NOT EXISTS idx_run_id  ON agent_logs(run_id)")
    conn.commit()
    return conn


def save_agent_log(
    run_id: str,
    exec_id: str,
    agent_id: str,
    input_content: str | None = None,
    output_content: str | None = None,
    runtime: int = 0,
    error_msg: str | None = None,
    extra: dict | None = None,
) -> None:
    """Persist an agent execution record."""
    try:
        conn = _get_conn()
        conn.execute(
            """INSERT INTO agent_logs
               (run_id, exec_id, agent_id, input_content, output_content, runtime, error_msg, extra)
               VALUES (?, ?, ?, ?, ?, ?, ?, ?)""",
            (
                run_id, exec_id, agent_id,
                input_content, output_content, runtime, error_msg,
                json.dumps(extra) if extra else None,
            ),
        )
        conn.commit()
        conn.close()
    except Exception as e:
        import sys
        print(f"[db] save_agent_log failed: {e}", file=sys.stderr)
```

---

## 5. 第三步：创建 Skills

### 5.1 主编排 Skill：`contract_draft`

```bash
mkdir -p src/copaw/agents/skills/contract_draft
```

创建 `src/copaw/agents/skills/contract_draft/SKILL.md`：

````markdown
---
name: contract_draft
description: "合同起草主编排智能体。当用户请求起草合同时调用（如'帮我起草一份采购合同'、'我要写一份服务合同'、'起草合同'等）。负责协调所有子智能体完成完整的合同起草流程，包括模板选择、参数填写和最终生成Word文件。"
metadata:
  {
    "copaw": {
      "emoji": "📋",
      "requires": {}
    }
  }
---

# 合同起草主编排智能体

协调子智能体完成完整合同起草流程，最终输出 Word 文件并推送前端渲染。

公共变量（从当前会话上下文获取）：
- `session_id`：当前会话 ID
- `user_id`：当前登录用户 ID
- `exec_id`：本次起草任务唯一 ID（用 `get_current_time` 辅助生成，或直接用 uuid）

---

## Step 1：生成执行 ID

在开始流程前，生成本次起草的唯一执行 ID（exec_id），整个流程透传。

---

## Step 2：获取合同模板

判断用户消息中是否包含附件文件：

### Case A：用户上传了附件（消息中含有文件）

1. 从消息附件获取文件路径
2. 调用工具 `read_file` 读取文件内容（或调用 `execute_shell_command` 用 pandoc 提取文本）
3. 调用 **contract_template_match** skill，`query_text=文件名称关键词`, `search_mode=name`
4. 展示相似模板列表，询问用户"是否使用以下模板，或直接用您上传的文件？"
5. 用户确认后，设置 `draft_template_text`（选中模板内容或上传文件内容）

### Case B：用户通过对话描述需求（无附件）

1. 从用户消息提取合同类型关键词（如"运维服务采购"、"软件开发"等）
2. 调用脚本匹配模板：
   ```bash
   execute_shell_command("python {skills_dir}/contract_template_match/scripts/template_match.py {session_id} {user_id} {exec_id} '{query_text}' name")
   ```
3. 解析脚本输出 JSON
4. **有匹配（total > 0）**：
   - 向用户展示模板列表（以 Markdown 表格展示）
   - 等待用户选择，例如"请选择模板编号"
   - 用户确认后，调用 Mock API 获取模板内容：
     ```bash
     execute_shell_command("curl -s http://localhost:9000/api/template/{template_id}/content")
     ```
   - 解析响应中的 `content` 字段，设置 `draft_template_text`
5. **无匹配（total = 0）**：
   - 告知用户"未找到匹配模板，将根据您的需求直接起草"
   - 设置 `draft_template_text = ""`（直接起草模式）

---

## Step 3：提取模板参数（有模板时）

若 `draft_template_text` 非空：

调用 **contract_template_params** skill，传入模板内容，获取参数 JSON。

展示参数表单（以 Markdown 格式列出所有字段），格式如下：

```
请填写以下合同参数：

**甲方信息**
- 甲方企业名称：___（必填）
- 统一社会信用代码：___（必填）

**乙方信息**
- 乙方企业名称：___（必填）

**合同条款**
- 合同金额（元）：___（必填）
- 合同金额（大写）：___（必填，可自动填写）
- 付款天数：___

**签署信息**
- 签署日期：___（必填，格式 YYYY-MM-DD）
- 甲方授权代表：___（必填）
- 乙方授权代表：___（必填）
```

等待用户填写。规则：
- 用户回复参数值后，记录到 `filled_params` 对象
- 检查所有 `required=true` 字段是否已填写
- 若有缺漏，提醒用户补全（列出缺少的字段）
- 若用户发出"开始起草"/"直接起草"/"跳过"等指令，跳过未填写的非必填字段，进入 Step 4

若 `draft_template_text` 为空（直接起草模式），跳过此步直接进入 Step 4。

---

## Step 4：生成合同 Word 文件

调用工具生成合同 Word 文件：

```python
markdown_to_word(
    markdown_content="...",  # 合同 Markdown 内容（由 contract_draft_llm skill 生成）
    output_filename="合同名称_草稿.docx"
)
```

然后保存文件：

```python
save_contract_file(
    file_path="...",
    contract_type="draft",
    exec_id="..."
)
```

获取 `saved_path`。

---

## Step 5：起草合同正文

调用 **contract_draft_llm** skill，传入：
- 模板内容 `draft_template_text`
- 用户填写的参数 `filled_params`
- 用户需求描述 `user_intent`（直接起草模式使用）

---

## Step 6：输出结果

向用户展示起草结果：

> 合同《{合同标题}》起草完成！
>
> 📄 已生成 Word 文件，可在右侧预览区查看。
>
> 如需修改，请告诉我需要调整的内容。

---

## 异常处理

- 模板 API 不可用：告知用户"模板服务暂时不可用，将根据您的描述直接起草合同"，切换到直接起草模式
- Word 生成失败：将 Markdown 合同内容直接返回给用户，提示可手动复制
- 用户中途取消：礼貌确认并停止流程
````

### 5.2 模板匹配 Skill：`contract_template_match`

```bash
mkdir -p src/copaw/agents/skills/contract_template_match/scripts
```

创建 `src/copaw/agents/skills/contract_template_match/SKILL.md`：

```markdown
---
name: contract_template_match
description: "合同起草时，根据用户提供的合同类型描述或上传的合同文件内容，调用模板向量检索 API 匹配最相似的合同模板列表，供用户选择确认。触发条件：用户说帮我起草合同、起草采购合同、找合同模板等相关描述时调用。"
metadata:
  {
    "copaw": {
      "emoji": "📄",
      "requires": {}
    }
  }
---

# 合同模板匹配智能体

根据用户需求检索匹配合同模板。

## 使用说明

调用脚本执行模板向量检索，传入以下参数：

    python scripts/template_match.py {session_id} {user_id} {exec_id} "{query_text}" {search_mode}

- `query_text`：用户描述的合同类型（file_chat 时）或合同文本摘要（file_attach 时）
- `search_mode`：`name`（名称检索）或 `content`（内容检索）

## 执行步骤

1. 从用户消息中提取合同类型/用途关键词作为 `query_text`
2. 调用上述脚本执行检索
3. 解析脚本 stdout 的 JSON 结果
4. 将结果通过 websocket 推送至前端展示

## 输出处理

- `total > 0`：展示模板列表，告知用户"找到以下相似合同模板，请选择确认"
- `total = 0`：告知用户"未找到匹配模板，将根据您的描述直接起草"
```

创建 `src/copaw/agents/skills/contract_template_match/scripts/template_match.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Contract template vector-search script.

Usage:
    python template_match.py <session_id> <user_id> <exec_id> <query_text> [search_mode]

search_mode: name (default) | content
"""
import json
import os
import sys
import time
import uuid

# Allow importing from skills/shared/
# __file__ is in skills/{skill_name}/scripts/, so ../../ → skills/ (contains shared/)
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), "../../")))
from shared.push import push
from shared.db import save_agent_log

try:
    import requests
except ImportError:
    requests = None

TEMPLATE_API = os.environ.get("TEMPLATE_API_BASE", "http://localhost:9000")


def search_templates(query: str, mode: str = "name", top_k: int = 10) -> dict:
    if requests is None:
        raise RuntimeError("requests library not installed")
    resp = requests.post(
        f"{TEMPLATE_API}/api/template/vector-search",
        json={"query": query, "mode": mode, "top_k": top_k},
        timeout=15,
    )
    resp.raise_for_status()
    return resp.json()


def main():
    if len(sys.argv) < 5:
        print(json.dumps({"error": "usage: template_match.py session_id user_id exec_id query_text [mode]"}))
        sys.exit(1)

    session_id  = sys.argv[1]
    user_id     = sys.argv[2]
    exec_id     = sys.argv[3]
    query_text  = sys.argv[4]
    search_mode = sys.argv[5] if len(sys.argv) > 5 else "name"

    run_id     = str(uuid.uuid4())
    start_time = time.time()

    push(session_id, user_id, "🔍 正在检索匹配合同模板...", msg_type="progress")

    try:
        result = search_templates(query_text, search_mode)
        runtime = int((time.time() - start_time) * 1000)

        save_agent_log(
            run_id=run_id,
            exec_id=exec_id,
            agent_id="contract_template_match",
            input_content=json.dumps({"query": query_text, "mode": search_mode}, ensure_ascii=False),
            output_content=json.dumps(result, ensure_ascii=False),
            runtime=runtime,
        )

        total = result.get("total", 0)
        push(session_id, user_id,
             f"✅ 找到 {total} 份相似合同模板" if total > 0 else "ℹ️ 未找到匹配模板",
             msg_type="result")

        print(json.dumps(result, ensure_ascii=False))

    except Exception as e:
        runtime = int((time.time() - start_time) * 1000)
        save_agent_log(
            run_id=run_id, exec_id=exec_id,
            agent_id="contract_template_match",
            input_content=json.dumps({"query": query_text}),
            output_content=None,
            runtime=runtime,
            error_msg=str(e),
        )
        push(session_id, user_id, f"❌ 模板检索失败：{e}", msg_type="error")
        print(json.dumps({"templates": [], "total": 0, "error": str(e)}))


if __name__ == "__main__":
    main()
```

### 5.3 参数提取 Skill：`contract_template_params`

```bash
mkdir -p src/copaw/agents/skills/contract_template_params
```

创建 `src/copaw/agents/skills/contract_template_params/SKILL.md`：

```markdown
---
name: contract_template_params
description: "合同起草流程中，当用户已选定合同模板后，解析模板内容，提取所有需要用户填写的空白字段（甲方信息、乙方信息、金额、日期、授权代表等），以JSON结构返回参数表单。由合同起草主智能体在确认模板后调用。"
metadata:
  {
    "copaw": {
      "emoji": "📝",
      "requires": {}
    }
  }
---

# 合同模板参数提取智能体

解析合同模板，提取所有待填写字段，生成前端表单 JSON。

## 提示词

接收合同模板全文（变量 `template_text`），识别所有空白字段，**严格按以下 JSON 格式输出，不要添加任何解释文字**：

```json
{
  "template_id": "TPL-xxx",
  "params": [
    {
      "param_id": "p001",
      "label": "字段名称",
      "field_name": "snake_case字段名",
      "value": null,
      "required": true,
      "type": "text | number | date | select | textarea",
      "placeholder": "填写提示",
      "options": null,
      "group": "分组（甲方信息 | 乙方信息 | 合同条款 | 签署信息）"
    }
  ]
}
```

## 识别规则

1. 【___】、（___）、《___》等空白括号内的内容 → 待填写字段
2. "年  月  日" 格式 → `date` 类型字段
3. "人民币（大写）___" → `text` 类型，`field_name=amount_cn`
4. 金额数字 "¥___" → `number` 类型，`field_name=amount`
5. 甲方/乙方的名称、地址、电话、信用代码、法人 → 分别提取，`group=甲方信息/乙方信息`
6. 违约金比例、付款天数等数值 → `number` 类型
7. 授权代表签字行 → `text` 类型，`group=签署信息`

## 执行方式

直接使用 LLM 分析模板文本，输出 JSON 参数表单。无需调用外部脚本。

收到模板内容后：
1. 仔细阅读模板全文
2. 按规则识别所有空白字段
3. 直接输出 JSON（不要 Markdown 代码块包裹，不要任何说明）
```

### 5.4 起草 LLM Skill：`contract_draft_llm`

```bash
mkdir -p src/copaw/agents/skills/contract_draft_llm
```

创建 `src/copaw/agents/skills/contract_draft_llm/SKILL.md`：

```markdown
---
name: contract_draft_llm
description: "合同起草流程最后阶段，当用户已填写好模板参数（或直接说开始起草）时，根据合同模板和参数生成完整合同正文（Markdown格式），然后调用工具生成Word文件。由合同起草主智能体在参数收集完成后调用。"
metadata:
  {
    "copaw": {
      "emoji": "✍️",
      "requires": {}
    }
  }
---

# 合同起草 LLM 智能体

根据模板和参数，生成完整合同正文并输出 Word 文件。

## 起草提示词

**你是一名专业合同起草专家。请根据以下信息起草一份完整、严谨的合同：**

### 合同模板
{template_text}

### 用户填写的参数
{params_json}

### 起草要求

1. 将参数值准确填入模板对应位置，不得遗漏任何参数
2. 金额大写自动转换（如 1500000 → 人民币壹佰伍拾万元整）
3. 保持合同的法律语言风格，不得随意改变条款实质内容
4. 使用标准 Markdown 格式输出：
   - 合同标题：`# 标题`
   - 各条款：`## 第X条 条款名称`
5. 合同末尾必须包含甲乙双方签署区块

**只输出 Markdown 格式合同正文，不要添加任何说明文字。**

---

## 执行步骤

1. 生成完整合同 Markdown 内容
2. 调用工具 `markdown_to_word` 将 Markdown 转为 Word 文件：
   ```
   markdown_to_word(markdown_content="...", output_filename="合同名称_草稿.docx")
   ```
3. 调用工具 `save_contract_file` 保存文件：
   ```
   save_contract_file(file_path="...", contract_type="draft", exec_id="...")
   ```
4. 获得 `file_path`，通过 websocket 推送至前端进行 Word 渲染
5. 向用户总结起草结果

## 无模板直接起草

若 `template_text` 为空，根据 `user_intent` 自行起草完整合同，包含所有标准合同条款。
```

---

## 6. 第四步：创建 Python Tools

### 6.1 创建 `src/copaw/agents/tools/markdown_to_word.py`

```python
# -*- coding: utf-8 -*-
"""Markdown to Word (.docx) conversion tool."""
import logging
import os
import re
from datetime import datetime
from pathlib import Path
from typing import Any

from agentscope.tool import ToolResponse
from agentscope.message import TextBlock

logger = logging.getLogger(__name__)

DRAFTS_DIR = Path(os.environ.get("CONTRACT_STORAGE_DIR", Path.home() / ".copaw" / "contracts" / "drafts"))


def _build_docx(markdown_content: str, output_path: Path) -> None:
    """Build a .docx file from Markdown content."""
    try:
        from docx import Document
        from docx.enum.text import WD_ALIGN_PARAGRAPH
        from docx.shared import Inches
    except ImportError as e:
        raise RuntimeError(f"python-docx not installed: {e}")

    doc = Document()
    section = doc.sections[0]
    section.left_margin = Inches(1.2)
    section.right_margin = Inches(1.2)
    section.top_margin = Inches(1.0)
    section.bottom_margin = Inches(1.0)

    lines = markdown_content.strip().split("\n")
    in_table = False
    table_rows: list[list[str]] = []

    def flush_table():
        nonlocal in_table, table_rows
        if not table_rows:
            return
        data_rows = [r for r in table_rows if not all(c.strip().startswith("-") for c in r)]
        if not data_rows:
            table_rows = []
            in_table = False
            return
        cols = max(len(r) for r in data_rows)
        tbl = doc.add_table(rows=len(data_rows), cols=cols)
        tbl.style = "Table Grid"
        for ri, row in enumerate(data_rows):
            for ci, cell_text in enumerate(row[:cols]):
                cell = tbl.rows[ri].cells[ci]
                cell.text = cell_text.strip()
                if ri == 0:
                    for run in cell.paragraphs[0].runs:
                        run.bold = True
        table_rows = []
        in_table = False

    for line in lines:
        stripped = line.rstrip()
        if stripped.startswith("|") and stripped.endswith("|"):
            in_table = True
            cells = [c.strip() for c in stripped[1:-1].split("|")]
            table_rows.append(cells)
            continue
        else:
            if in_table:
                flush_table()

        if stripped.startswith("### "):
            doc.add_heading(stripped[4:], level=3)
        elif stripped.startswith("## "):
            doc.add_heading(stripped[3:], level=2)
        elif stripped.startswith("# "):
            p = doc.add_heading(stripped[2:], level=1)
            p.alignment = WD_ALIGN_PARAGRAPH.CENTER
        elif stripped.startswith("---") and len(stripped.strip("-")) == 0:
            doc.add_paragraph("─" * 40)
        elif stripped.startswith("- ") or stripped.startswith("* "):
            doc.add_paragraph(stripped[2:], style="List Bullet")
        elif re.match(r"^\d+\. ", stripped):
            doc.add_paragraph(re.sub(r"^\d+\. ", "", stripped), style="List Number")
        elif stripped:
            p = doc.add_paragraph()
            parts = re.split(r"\*\*(.+?)\*\*", stripped)
            for i, part in enumerate(parts):
                run = p.add_run(part)
                if i % 2 == 1:
                    run.bold = True
        else:
            doc.add_paragraph("")

    if in_table:
        flush_table()
    doc.save(str(output_path))


async def markdown_to_word(
    markdown_content: str,
    output_filename: str = "",
    **kwargs: Any,
) -> ToolResponse:
    """Convert Markdown contract content to a Word .docx file.

    Args:
        markdown_content: Full Markdown text of the contract.
        output_filename:  Desired filename (without path). Auto-generated if empty.

    Returns:
        JSON: { file_path, file_size, filename }
    """
    import json

    if not markdown_content or not markdown_content.strip():
        return ToolResponse(
            content=[TextBlock(type="text", text='{"error": "markdown_content is empty"}')]
        )

    if not output_filename:
        title_match = re.search(r"^#\s+(.+)$", markdown_content, re.MULTILINE)
        title = title_match.group(1).strip() if title_match else "合同草稿"
        ts = datetime.now().strftime("%Y%m%d_%H%M%S")
        output_filename = f"{title}_{ts}.docx"

    if not output_filename.endswith(".docx"):
        output_filename += ".docx"

    DRAFTS_DIR.mkdir(parents=True, exist_ok=True)
    output_path = DRAFTS_DIR / output_filename

    try:
        _build_docx(markdown_content, output_path)
        file_size = output_path.stat().st_size
        result = {"file_path": str(output_path), "file_size": file_size, "filename": output_filename}
        logger.info("markdown_to_word: created %s (%d bytes)", output_path, file_size)
        return ToolResponse(content=[TextBlock(type="text", text=json.dumps(result, ensure_ascii=False))])
    except Exception as e:
        logger.exception("markdown_to_word failed: %s", e)
        return ToolResponse(content=[TextBlock(type="text", text=json.dumps({"error": str(e)}))])
```

### 6.2 创建 `src/copaw/agents/tools/save_contract_file.py`

```python
# -*- coding: utf-8 -*-
"""Contract file storage tool."""
import json
import logging
import os
import shutil
from datetime import datetime
from pathlib import Path
from typing import Any

from agentscope.tool import ToolResponse
from agentscope.message import TextBlock

logger = logging.getLogger(__name__)

STORAGE_BASE = Path(os.environ.get("CONTRACT_STORAGE_DIR", str(Path.home() / ".copaw" / "contracts")))
COPAW_API = os.environ.get("COPAW_API_BASE", "http://127.0.0.1:8088")


async def save_contract_file(
    file_path: str,
    contract_type: str = "draft",
    exec_id: str = "",
    original_name: str = "",
    **kwargs: Any,
) -> ToolResponse:
    """Save a contract Word file to permanent storage.

    Args:
        file_path:      Source file path.
        contract_type:  "draft" | "review" | "compare"
        exec_id:        Main execution ID for traceability.
        original_name:  Optional filename override.

    Returns:
        JSON: { saved_path, file_url, file_size, filename }
    """
    src = Path(file_path)
    if not src.exists():
        return ToolResponse(
            content=[TextBlock(type="text", text=json.dumps({"error": f"file not found: {file_path}"}))]
        )

    now = datetime.now()
    dest_dir = STORAGE_BASE / contract_type / now.strftime("%Y") / now.strftime("%m")
    dest_dir.mkdir(parents=True, exist_ok=True)

    filename = original_name or src.name
    prefix = exec_id[:8] if exec_id else now.strftime("%H%M%S")
    dest_name = f"{prefix}_{filename}" if not filename.startswith(prefix) else filename
    dest_path = dest_dir / dest_name

    counter = 1
    while dest_path.exists():
        stem = dest_name.rsplit(".", 1)[0]
        ext = dest_name.rsplit(".", 1)[-1] if "." in dest_name else ""
        dest_path = dest_dir / f"{stem}_{counter}.{ext}"
        counter += 1

    shutil.copy2(str(src), str(dest_path))
    file_size = dest_path.stat().st_size
    rel = dest_path.relative_to(STORAGE_BASE.parent)
    file_url = f"{COPAW_API}/files/{rel}"

    result = {"saved_path": str(dest_path), "file_url": file_url, "file_size": file_size, "filename": dest_name}
    logger.info("save_contract_file: saved to %s (%d bytes)", dest_path, file_size)
    return ToolResponse(content=[TextBlock(type="text", text=json.dumps(result, ensure_ascii=False))])
```

---

## 7. 第五步：注册 Tools 到框架

### 7.1 修改 `src/copaw/agents/tools/__init__.py`

在 `get_weather` 相关行之后追加：

```python
from .markdown_to_word import markdown_to_word
from .save_contract_file import save_contract_file
```

并在 `__all__` 列表末尾追加：

```python
"markdown_to_word",
"save_contract_file",
```

### 7.2 修改 `src/copaw/agents/react_agent.py`

**导入部分**，在 `get_weather` 导入行后追加：

```python
from .tools import (
    ...
    get_weather,
    markdown_to_word,       # ← 新增
    save_contract_file,     # ← 新增
    ...
)
```

**注册部分**，找到 `_create_toolkit` 方法中 `toolkit.register_tool_function(get_weather)` 后追加：

```python
toolkit.register_tool_function(markdown_to_word)
toolkit.register_tool_function(save_contract_file)
```

---

## 8. 第六步：创建 Mock 模板 API 服务

创建 `src/copaw/agents/skills/shared/mock_template_api.py`：

> 该文件提供 `POST /api/template/vector-search` 和 `GET /api/template/{id}/content` 两个接口，用于开发调试。生产环境替换为真实 API 即可，无需修改 skill 代码。

```python
# -*- coding: utf-8 -*-
"""Mock template vector-search API server (for development/testing).

Run:
    python mock_template_api.py
"""
import json
from http.server import BaseHTTPRequestHandler, HTTPServer

MOCK_TEMPLATES = [
    {
        "template_id": "TPL-2024-001",
        "template_name": "IT运维服务采购合同模板",
        "template_type": "采购合同",
        "version": "V2.1",
        "similarity": 0.92,
        "file_path": "/templates/2024/TPL-2024-001.docx",
        "description": "适用于IT运维类服务采购场景，含SLA条款",
    },
    {
        "template_id": "TPL-2024-002",
        "template_name": "软件开发服务合同模板",
        "template_type": "服务合同",
        "version": "V1.3",
        "similarity": 0.85,
        "file_path": "/templates/2024/TPL-2024-002.docx",
        "description": "适用于软件定制开发场景，含验收条款和知识产权条款",
    },
    {
        "template_id": "TPL-2023-008",
        "template_name": "年度框架采购合同模板",
        "template_type": "框架合同",
        "version": "V3.0",
        "similarity": 0.78,
        "file_path": "/templates/2023/TPL-2023-008.docx",
        "description": "适用于年度框架采购，支持多次下单执行",
    },
]

MOCK_TEMPLATE_CONTENT = """# {template_name}

合同编号：【_______________】

**甲方（采购方）**：【_______________】
统一社会信用代码：【_______________】
法定代表人：【_______________】
联系地址：【_______________】
联系电话：【_______________】

**乙方（供应商）**：【_______________】
统一社会信用代码：【_______________】
法定代表人：【_______________】
联系地址：【_______________】
联系电话：【_______________】

---

## 第一条 项目概述

1.1 服务内容：【_______________】
1.2 服务期限：自 【___年___月___日】 起至 【___年___月___日】 止

## 第二条 合同金额

2.1 合同总价：人民币（大写）【_______________】元整（¥【_______________】元）
2.2 付款条件：验收合格后【___】个自然日内完成付款

## 第三条 甲方责任

3.1 甲方应按约定及时向乙方提供所需资料及工作条件；
3.2 甲方应按合同约定及时支付合同款项。

## 第四条 乙方责任

4.1 乙方应按约定时间和质量标准完成服务交付；
4.2 乙方应对本合同内容严格保密。

## 第五条 验收标准

5.1 验收标准：【_______________】
5.2 验收周期：交付后【___】个工作日内完成验收

## 第六条 违约责任

6.1 任何一方违约，应向对方支付合同总价【___】%的违约金。

## 第七条 争议解决

8.1 争议提交【_______________】仲裁委员会仲裁。

---

**甲方（盖章）**：__________________    **乙方（盖章）**：__________________

**授权代表签字**：__________________    **授权代表签字**：__________________

**签署日期**：____年____月____日         **签署日期**：____年____月____日
"""


class MockHandler(BaseHTTPRequestHandler):
    def log_message(self, format, *args):
        pass

    def do_POST(self):
        if self.path == "/api/template/vector-search":
            length = int(self.headers.get("Content-Length", 0))
            body = json.loads(self.rfile.read(length) or b"{}")
            query = body.get("query", "").lower()
            top_k = body.get("top_k", 5)
            results = []
            for t in MOCK_TEMPLATES:
                if any(kw in query for kw in ["运维", "维护", "it"]) and t["template_id"] == "TPL-2024-001":
                    results.append(t)
                elif any(kw in query for kw in ["软件", "开发", "系统"]) and t["template_id"] == "TPL-2024-002":
                    results.append(t)
                elif any(kw in query for kw in ["框架", "采购", "年度"]):
                    results.append(t)
            if not results:
                results = MOCK_TEMPLATES[:2]
            results = results[:top_k]
            self._json(200, {"total": len(results), "templates": results})
        else:
            self._json(404, {"error": "not found"})

    def do_GET(self):
        if self.path.startswith("/api/template/") and self.path.endswith("/content"):
            tid = self.path.split("/")[3]
            tmpl = next((t for t in MOCK_TEMPLATES if t["template_id"] == tid), None)
            name = tmpl["template_name"] if tmpl else "合同模板"
            content = MOCK_TEMPLATE_CONTENT.format(template_name=name)
            self._json(200, {"template_id": tid, "template_name": name, "content": content})
        else:
            self._json(404, {"error": "not found"})

    def _json(self, code, data):
        body = json.dumps(data, ensure_ascii=False).encode()
        self.send_response(code)
        self.send_header("Content-Type", "application/json; charset=utf-8")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)


if __name__ == "__main__":
    server = HTTPServer(("0.0.0.0", 9000), MockHandler)
    print("[MockTemplateAPI] listening on http://0.0.0.0:9000")
    server.serve_forever()
```

---

## 9. 第七步：同步 Skills 并启动服务

### 9.1 安装依赖

```bash
# 确保 python-docx 已安装（在 CoPaw 的 Python 环境中）
pip install python-docx
```

### 9.2 同步 Skills 到 active_skills

CoPaw 从 `~/.copaw/active_skills/` 加载已启用的 skills，需要将新 skills 复制过去：

```bash
ACTIVE_DIR=~/.copaw/active_skills
SKILLS_SRC=/path/to/CoPaw/src/copaw/agents/skills  # 替换为实际路径

for skill in contract_draft contract_template_match contract_template_params contract_draft_llm shared; do
  rm -rf "$ACTIVE_DIR/$skill"
  cp -r "$SKILLS_SRC/$skill" "$ACTIVE_DIR/$skill"
  echo "Copied: $skill"
done
```

> **注意**：`shared` 目录也需要复制，脚本运行时通过相对路径 `../../shared` 引用它。

### 9.3 启动 Mock API（后台）

```bash
nohup python src/copaw/agents/skills/shared/mock_template_api.py > /tmp/mock_api.log 2>&1 &
# 验证
curl -s http://localhost:9000/api/template/TPL-2024-001/content | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['template_name'])"
```

### 9.4 启动/重启 CoPaw

```bash
# 停止旧进程
pkill -f "copaw app"
sleep 2

# 重启（--reload 模式，代码修改后自动重载）
nohup copaw app --reload > /tmp/copaw.log 2>&1 &

# 等待启动
sleep 10
curl -s http://127.0.0.1:8088/api/version
```

### 9.5 一键启动脚本

创建 `scripts/start_contract_services.sh`：

```bash
#!/bin/bash
# 合同起草服务一键启动脚本

SCRIPTS_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPTS_DIR")"
ACTIVE_DIR=~/.copaw/active_skills
SKILLS_SRC="$PROJECT_DIR/src/copaw/agents/skills"

echo "=== 合同起草服务启动 ==="

# 1. 启动 Mock Template API
if ! curl -s http://localhost:9000/api/template/TPL-2024-001/content > /dev/null 2>&1; then
  echo "[1/3] 启动 Mock Template API (port 9000)..."
  nohup python "$SKILLS_SRC/shared/mock_template_api.py" > /tmp/mock_api.log 2>&1 &
  sleep 1
else
  echo "[1/3] Mock Template API 已在运行"
fi

# 2. 同步 skills
echo "[2/3] 同步 skills 到 active_skills..."
for skill in contract_draft contract_template_match contract_template_params contract_draft_llm shared; do
  rm -rf "$ACTIVE_DIR/$skill"
  cp -r "$SKILLS_SRC/$skill" "$ACTIVE_DIR/$skill"
done

# 3. 重启 CoPaw
echo "[3/3] 重启 CoPaw..."
pkill -f "copaw app" 2>/dev/null
sleep 2
nohup copaw app --reload > /tmp/copaw.log 2>&1 &

echo "=== 启动完成 ==="
echo "  Mock API: http://localhost:9000"
echo "  CoPaw:    http://localhost:8088"
```

```bash
chmod +x scripts/start_contract_services.sh
```

---

## 10. 验证测试

### 10.1 验证接口

```bash
# 1. CoPaw 版本
curl -s http://127.0.0.1:8088/api/version

# 2. 内部推送接口
curl -s -X POST http://127.0.0.1:8088/api/console/internal-push \
  -H "Content-Type: application/json" \
  -d '{"session_id":"test","user_id":"u1","text":"测试推送","msg_type":"progress"}'
# 期望：{"ok":true}

# 3. Mock 模板 API
curl -s -X POST http://localhost:9000/api/template/vector-search \
  -H "Content-Type: application/json" \
  -d '{"query":"采购合同","top_k":3}'
# 期望：{"total":3,"templates":[...]}

# 4. 合同文件访问（先生成一个）
# 启动成功后访问：http://127.0.0.1:8088/files/contracts/
```

### 10.2 验证脚本运行

```bash
python ~/.copaw/active_skills/contract_template_match/scripts/template_match.py \
  "sess_001" "user_001" "exec_001" "IT运维采购合同" name
# 期望：{"total":3,"templates":[...]}
```

### 10.3 验证日志入库

```bash
python3 -c "
import sqlite3
conn = sqlite3.connect(os.path.expanduser('~/.copaw/agent_logs.db'))
rows = conn.execute('SELECT agent_id, exec_id, runtime FROM agent_logs ORDER BY id DESC LIMIT 5').fetchall()
for r in rows: print(r)
"
```

### 10.4 验证 Skills 注册

```bash
curl -s http://127.0.0.1:8088/api/skills | python3 -c "
import json, sys
data = json.load(sys.stdin)
names = [s['name'] for s in data]
for n in names:
    if 'contract' in n:
        print('✅', n)
"
# 期望输出：
# ✅ contract_draft
# ✅ contract_template_match
# ✅ contract_template_params
# ✅ contract_draft_llm
```

### 10.5 对话测试

打开 `http://localhost:8088`，在聊天框输入：

```
帮我起草一份IT运维服务采购合同
```

**预期流程**：
1. CoPaw 触发 `contract_draft` skill
2. 自动调用脚本检索模板，展示 3 个匹配模板
3. 用户选择模板后展示参数填写表单
4. 用户填写完毕后，LLM 生成完整合同 Markdown
5. 自动生成 Word 文件并返回文件路径

---

## 11. 生产环境替换说明

| 组件 | 开发配置 | 生产替换 |
|---|---|---|
| 模板向量 API | `mock_template_api.py`（本地 9000）| 替换 `TEMPLATE_API_BASE` 环境变量指向真实 API |
| 日志数据库 | SQLite（`~/.copaw/agent_logs.db`）| 设置 `AGENT_LOG_DB` 为 PostgreSQL 连接串，修改 `db.py` 的连接逻辑 |
| 文件存储 | 本地目录（`~/.copaw/contracts/`）| 设置 `CONTRACT_STORAGE_DIR` 为 NAS/OSS 挂载路径 |
| CoPaw 端口 | `http://127.0.0.1:8088` | 设置 `COPAW_API_BASE` 环境变量 |
| Python 环境 | Miniconda 3.11 | 确保 `python-docx` 和 `requests` 已安装 |

### 环境变量汇总

```bash
# .env 或系统环境变量
TEMPLATE_API_BASE=http://your-template-api.com   # 模板向量检索 API
COPAW_API_BASE=http://127.0.0.1:8088             # CoPaw 服务地址（脚本回调用）
CONTRACT_STORAGE_DIR=/data/contracts              # 合同文件存储根目录
AGENT_LOG_DB=/data/agent_logs.db                  # 日志 SQLite 路径（或 PostgreSQL URI）
```

---

*文档生成时间：2026-03-04 | CoPaw 版本：0.0.4b2*
