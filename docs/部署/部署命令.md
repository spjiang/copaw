# 重启 CoPaw

```bash
nohup /Users/jiangshengping/miniconda3/bin/copaw app > /tmp/copaw.log 2>&1 &
echo "PID: $!"
```

# 验证服务

```bash
curl -s http://localhost:8088/ | head -2
```

# 验证文件上传接口

```bash

curl -s -X POST http://localhost:8088/api/files/upload \
  -F "file=@/Users/jiangshengping/wwwroot/rtzh/wwwroot/CoPaw/docs/prompt/合同起草对话.md" \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print('✅ 上传成功:', d.get('url',''))"

```

# 验证 skills

```bash
curl -s "http://localhost:8088/api/skills" | python3 -c "
import json,sys
data = json.load(sys.stdin)
print(f'共 {len(data)} 个 skill：')
for s in data:
    mark = '✅' if 'contract' in s['name'] else '  '
    print(f'  {mark} {s[\"name\"]}')
"
```

# copaw工作目录

```bash
/Users/jiangshengping/.copaw

```


# 文件上传地址

/Users/jiangshengping/.copaw/file_store/

```bash
ls -lt ~/.copaw/file_store/uploads/

```
